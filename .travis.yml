sudo: required
language: node_js

node_js:
  - "7"

before_script:
  - npm install -g reaction-cli
  - curl https://install.meteor.com/ | sh
  - meteor npm install
  - sudo chown travis -Rh .meteor/local /home/travis/.config

script:
  - reaction test

notifications:
  slack: olgaverkkokauppa:VyuKQR5JBQpHmyOWE6bPRBZp